import type { FC } from 'react';

import NavBar from '@/components/Containers/NavBar';

const WithNavBar: FC = () => {
  return (
    <div>
      <NavBar />
    </div>
  );
};

export default WithNavBar;
